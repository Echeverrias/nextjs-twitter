import css from 'styled-jsx/css'

export default css`
  button {
    background: rgba(0, 0, 0, 0.4);
    border: 0;
    outline: 0;
    border-radius: 999px;
    color: #fff;
    font-size: 24px;
    width: 32px;
    height: 32px;
  }

  button:hover {
    background: rgba(0, 0, 0, 0.3);
    cursor: pointer;
  }
`
